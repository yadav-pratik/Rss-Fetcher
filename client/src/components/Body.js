import React from "react"

import ArticlesList from "./Articles/ArticlesList"

const Body = (props) => {
    return (
        <div className="container">
            <ArticlesList />
        </div>
    )
}

export default Body